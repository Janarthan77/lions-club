import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['volitionary-noncovetously-hipolito.ngrok-free.dev'],
};

export default nextConfig;
